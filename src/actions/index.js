import * as types from './../constants/ActionTypes';
import firebase from './../components/Firebase'
import Firebase from './../components/Firebase';

export const doCreateUserWithEmailAndPassword = (email, passwordOne) => {
  Firebase.
  username;
}